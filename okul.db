PRAGMA foreign_keys = ON;


CREATE TABLE IF NOT EXISTS students(

    id INTEGER PRIMARY KEY AUTOINCREMENT,
    student_number TEXT UNIQUE NOT NULL,
    full_name TEXT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
);

CREATE TABLE IF NOT EXISTS courses(

    id INTEGER PRIMARY KEY AUTOINCREMENT,
    course_code TEXT UNIQUE NOT NULL,
    title TEXT NOT NULL,
    credit INTEGER NOT NULL, CHECK (credit > 0),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
);

CREATE TABLE IF NOT EXISTS enrollments(    ---Olaylar burada gerçkeleşitor.

    student_id INTEGER NOT NULL ,
   
    course_id INTEGER NOT NULL,
    enrollment_code DATETIME DEFAULT CURRENT_TIMESTAMP,
    final_grade INTEGER CHECK (final_grade >= 0 AND final_grade <= 100),
    PRIMARY KEY (student_id, course_id),
    FOREIGN KEY (student_id) REFERENCES students(id) ON DELETE CASCADE,
    FOREIGN KEY (course_id) REFERENCES courses(id) ON DELETE CASCADE,
    UNIQUE (student_id, course_id)

);

INSERT INTO students (student_number, full_name) VALUES
('S001', 'Harun Kara'),


INSERT INTO courses (course_code, title, credit) VALUES
('C001', 'Mathematics', 3),


INSERT INTO enrollments (student_id, course_id, final_grade) VALUES
(1, 1, 85);


---TEST1 COURSE DELETION---
DELETE FROM courses WHERE id = 1;

DELETE FROM students WHERE id = 2;